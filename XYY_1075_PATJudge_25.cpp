#include<cstdio>
#include<algorithm>
#include<vector>

using namespace std;
int N,K,M;

int main() {
  scanf("%d%d%d",&N,&K,&M);
  int p[K+1] = {0};
  for(int i=0;i<K;i++) {
    scanf("%d",&p[i]);
  }
  for(int i=0;i<M;i++) {
    
  }

  return 0;
}